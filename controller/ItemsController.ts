import type { RouterContext } from "https://deno.land/x/oak@v17.1.2/router.ts";
import { APIController } from "./APIController.ts";

export class ItemsController extends APIController {
    handleDelete(context: RouterContext<string>): void {
    }

    handleGet(context: RouterContext<string>): void {
    }

    handlePost(context: RouterContext<string>): void {
    }

    handlePut(context: RouterContext<string>): void {
    }
}